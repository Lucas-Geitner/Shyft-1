<h1>Vos <%= @organisation.group_name %>s</h1>
<% @organisation.groups.each do |g| %>
  <h2><%= g.name %></h2>
  <ul class="list-inline list-unstyled">
    <li><%= link_to "Plannings", group_plannings_path(g) %></li>
    <li><%= link_to "Espace Administratif", admin_space_path(g) %></li>
  </ul>
<% end %>
